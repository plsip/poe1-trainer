FROM node:22-alpine

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci

# Source code is mounted as a volume at runtime.
# node_modules stays in the image layer via the named volume in compose.
CMD ["npm", "run", "dev"]

# Prompt 11: Docker and Local Development Environment

```text
Prepare the local development environment for a React + Go + PostgreSQL Path of Exile 1 campaign trainer using Docker Compose.

[Paste shared context here]

I want:
- Go backend,
- React frontend,
- PostgreSQL database,
- practical local development setup,
- ability to run without GGG OAuth configured,
- sensible environment variable layout,
- seed data for an example Storm Burst Totems build.

Deliver:
1. environment structure,
2. backend Dockerfile,
3. frontend Dockerfile,
4. Docker Compose for development,
5. migrations and seed strategy,
6. local run instructions,
7. notes on how to mount or configure the local PoE1 Client.txt path safely.

The application should work locally first, even without full GGG API integration.

Respond in Polish.
Use English for code, identifiers, API names, database schema names, and technical labels unless explicitly asked otherwise.
Keep user-facing explanations in Polish.
```